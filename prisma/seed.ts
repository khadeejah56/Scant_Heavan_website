// Product catalog is static (see data/products.ts) and not stored in the
// database, so there is no seed data required for a fresh dev database yet.
console.log("Nothing to seed — the product catalog lives in data/products.ts.");
